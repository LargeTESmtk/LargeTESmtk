within LargeTESmtk.Components.GroundDomain.GridUnits.Utilities;
package GridErrorEval "Package with records for grid error evaluation"
  extends Modelica.Icons.RecordsPackage;
  annotation (Documentation(info="<html>

<p>
This package includes various records for evaluating grid errors resulting from the non-uniform grid (i.e., the systematic increase in node size in one direction), 
as well as the division into grid segments.
</p>

</html>"));
end GridErrorEval;
