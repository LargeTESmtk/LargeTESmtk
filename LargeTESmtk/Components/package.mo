within LargeTESmtk;
package Components "Storage component models"
  extends Modelica.Icons.VariantsPackage;
  annotation (Documentation(info="<html>

<p>
This package includes the component models of the pre-built storage models, as well as the core component models for creating new storage models.
The package is subdivided into two main subpackages for the fluid and ground domain. 
</p>

</html>"));
end Components;
